name: Lint, Build and Test Rain
on:
  pull_request: 
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main
jobs:
  lint-repo:
    name: Lint Rain Repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.20.0
      
      - name: Use Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install

      - name: Lint Respository
        run: pnpm lint


  build-test-rain:
    name: Build and Test Rain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.20.0
      
      - name: Use Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm --filter @rain2d/rain install

      - name: Build Rain
        run: pnpm --filter @rain2d/rain build

      - name: Test Rain
        run: pnpm --filter @rain2d/rain test

      - name: Build Rain View
        run: pnpm --filter @rain2d/view build

      - name: Build Rain Scenes
        run: pnpm --filter @rain2d/scenes build

      - name: Build Rain Tweening
        run: pnpm --filter @rain2d/tweening build


  build-projects:
    name: Build Rain Projects
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.20.0
      
      - name: Use Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install

      - name: Build @rain2d/rain
        run: pnpm --filter @rain2d/rain build

      # Bunnies Project
      - name: Build @rain2d-projects/bunnies
        run: pnpm --filter @rain2d-projects/bunnies build